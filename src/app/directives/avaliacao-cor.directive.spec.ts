import { AvaliacaoColorDirective } from './avaliacao-cor.directive';

describe('AvaliacaoCorDirective', () => {
  it('should create an instance', () => {
    const directive = new AvaliacaoColorDirective();
    expect(directive).toBeTruthy();
  });
});
